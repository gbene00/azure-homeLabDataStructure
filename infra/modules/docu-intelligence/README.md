# docu-intelligence module

Creates:
- Azure AI Document Intelligence (Form Recognizer) resource

Outputs:
- endpoint
- primary access key (sensitive)

Used by:
- Azure Function to analyze uploaded documents and produce structured JSON