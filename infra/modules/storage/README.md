# storage module

Creates:
- Storage Account for data
- Blob containers: raw + processed
- Blob protection: versioning + soft delete retention

Used for:
- raw file uploads (raw container)
- processed JSON outputs (processed container)